# Security

SnapKeep archives files from local directories. Backup archives may contain
sensitive information if exclusion rules are configured incorrectly.

The project is being designed with secure defaults for common secret files,
but users should always review the contents and destination of backups before
sharing or uploading them.

Do not publish backup archives containing credentials, API keys, private keys,
tokens, or other sensitive data.
